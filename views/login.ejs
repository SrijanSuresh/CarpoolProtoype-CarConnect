<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <!-- Include Tailwind CSS -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <!-- CSS Fonts below -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Bungee+Spice&family=Caveat&display=swap" rel="stylesheet">
    <style>
        /* Center the container both horizontally and vertically */
        .container {
            display: flex;
            flex-direction: column; /* Stack items vertically */
            justify-content: center; /* Center items horizontally */
            align-items: center; /* Center items vertically */
            height: 100vh; /* Full height of viewport */
            background-color: #296bf0; /* Optional: Set background color */
        }
    
        /* Custom font families */
        .font-bungee {
            font-family: 'Bungee Spice', cursive;
        }
        .font-caveat {
            font-family: 'Caveat', cursive;
        }
    
        /* Form container styling */
        .form-container {
            background-color: #fff;
            padding: 2rem;
            border-radius: 0.5rem;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            margin-top: 2rem; /* Adjust margin as needed */
            max-width: 400px; /* Set maximum width */
            width: 90%; /* Set width to 90% */
        }
        .small-text {
            font-size: 30px;
        }

    </style>
    
</head>
<body>
    <div class="container">
        <!-- Welcome text -->
        <h1 class="font-bungee text-4xl font-semibold mb-8 text-white">Welcome! Please sign in to continue.</h1>

        <!-- Form -->
        <form action="/login" method="POST" class="form-container flex flex-col items-center space-y-9">
            <label for="username" class="text-sm font-regular small-text">Username</label>
            <input type="text" id="username" name="username" required class="border border-gray-300 rounded-md py-2 px-3 focus:outline-none focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500">
        
            <label for="password" class="text-sm font-regular small-text">Password</label>
            <input type="password" id="password" name="password" required class="border border-gray-300 rounded-md py-2 px-3 focus:outline-none focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500">
        
            <button type="submit" class="bg-indigo-600 hover:bg-indigo-700 text-white font-semibold py-2 px-4 rounded-md focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 font-regular large-text">Sign In</button>
        </form>        
        
    </div>
</body>
</html>
